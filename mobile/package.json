{
  "name": "hongguo-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "红果短剧 iOS App（Capacitor 壳，连接本机或云端 Python 后端）",
  "scripts": {
    "build:web": "node scripts/copy-web.js",
    "cap:sync": "npm run build:web && npx cap sync ios",
    "cap:ios": "npx cap open ios",
    "cap:run:ios": "npx cap run ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.2",
    "@capacitor/camera": "^6.1.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/filesystem": "^6.0.3",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/status-bar": "^6.0.2",
    "@capacitor-community/media": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.7.2"
  }
}
